# Python - Server-Side Rendering

This project explores server-side rendering using Python and Flask with the Jinja2 templating engine. It covers reading data from JSON, CSV, and SQLite sources and dynamically generating HTML pages.
